# Copy this file to .streamlit/secrets.toml and fill in your values.
# On Streamlit Cloud / HF Spaces: paste these into the Secrets panel instead.
#
# NEVER commit secrets.toml to git — it is in .gitignore.

[huggingface]
# Your Hugging Face model repository (e.g., "akiroussama/rakuten-models")
repo_id = "akiroussama/rakuten-models"

# Access token (only needed if the repo is PRIVATE)
# Generate at: https://huggingface.co/settings/tokens
# token = "hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
